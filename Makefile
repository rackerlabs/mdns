help:
		@echo ""
		@echo "build          - builds a mdns executable"
		@echo "test           - runs tests"
		@echo "run            - runs mdns"
		@echo "clean          - cleans up built binaries"
		@echo ""

build:
		@echo "Build!"

test:
		@echo "Test!"

